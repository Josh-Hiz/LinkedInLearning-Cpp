
//Include is a pre-processor directive, iostream is part of
//the C++ standard library that has objects and functions
//to print text to the screen and receive text from the keyboard
#include <iostream>

//Function that is executed by default and must return an integer

using namespace std;

int main() {
    cout << "Hello world!" << endl;
    return 0;
}
